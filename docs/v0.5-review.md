# v0.5.0 Review — Changes to Implement

## 5 Mandatory Changes

1. **Keep Zod optional** — Already done. `SchemaValidator` is framework-agnostic.
2. **Make routing explainable** — Add `reasons: string[]` and `candidates` to `RoutingResult`.
3. **Separate JSON validity from schema validity** — Already done in architecture.
4. **Auto-connect output → router** — When `output` is set, auto-set `needsStructuredOutput: true`.
5. **Auto-repair respects cost policy** — Track cumulative cost across repair attempts.

## Additional Improvements

- Controlled `TaskType` taxonomy instead of free string
- Rename to "Policy-Based Model Router" in docs
- Enhanced `RoutingResult` with reasons and rejected candidates
